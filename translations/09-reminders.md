# 提醒

Omarchy 内置了简单的倒计时提醒功能，并可以附带文字消息。使用 `Super + Ctrl + R` 创建提醒，使用 `Super + Ctrl + Alt + R` 查看所有提醒，使用 `Super + Ctrl + Shift + R` 清除全部提醒。所有功能也可以通过 _Trigger > Reminder_ 访问。命令行方式是 `omarchy reminder 7 'Tea ready'`。

![reminders](images/reminders.webp)
